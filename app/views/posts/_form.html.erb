<%= form_for @post do |f| %>
  <%= f.text_field :title, placeholder: "Title", required: true %> <br />
  <%= f.text_area :content, placeholder: "Add stuff here", required: true %> <br />
  <%= f.submit "Submit" %>
<% end %>
